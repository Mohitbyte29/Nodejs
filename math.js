const add = (a, b) => (a + b);
const sub = (a, b) => (a - b);
const mul = (a, b) => (a * b);
const div = (a, b) => (a / b);
const PI = 3.214;

// module.exports.add = add;
// module.exports.mul = mul;
// module.exports.subs = subs;
// module.exports.divs = divs;
// module.exports.PI = PI;

module.exports = {add, mul, sub, div, PI};


